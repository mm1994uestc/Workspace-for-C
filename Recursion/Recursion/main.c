/***********************************************************************
NowDays i am going to develop the C programming skill to get something fr
om the IEEE Extreme Competition 10.0. The Training is begin from the Algo
rithm named 'Recursion'.
   Edition 1.0     Time:2016.10.20      Location:GISPALAB Office
************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

int main()
{
    int N,M,k;//N-Row  M-Clo  C-Characters
    char Character;
    char * Character_Adress=&Character;
    printf("Please enter the Square of the size N-Clo M-Row k-Characters:");
    scanf("%d,%d,%d",&N,&M,&k);
    if(!(1<=N && N<=100) || !(1<=M && M<=100) || !(3<=k && k<=95))
    {
        printf("The wrong input!");
        return 0;
    }
    for(int i=1;i<=k,i++)
    {
        printf("Please enter the Character:");//Input for Characters.
        scanf("%c",&Character);
        for
    }
    int x;
    printf("Please enter How many lines would you want to show:");
    scanf("%d",&x);
    char * Input_Adress=;
    for(int j=1;j<=x,j++)
    {
        printf("Please enter the %d line's context:",j);
        scanf("%c",)
    }
    return 0;
}
